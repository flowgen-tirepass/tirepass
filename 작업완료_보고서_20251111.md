# 결제 시스템 긴급 조치 완료 보고서

**일시**: 2025년 11월 11일
**담당**: Claude Code
**상황**: 광주 업체 미결제 주문 발생 및 긴급 조치

---

## 📊 문제 상황

### 발생 사실
- **일시**: 2025년 11월 11일 오후 2시경
- **주문번호**: ORD20251111008, ORD20251111009
- **고객**: 타이어프로 하남점 (0-0-0002)
- **금액**: 각 83,160원
- **증상**:
  - 모바일에서 결제 완료 화면 표시
  - 관리자 페이지에서 "미결제" 상태
  - Payment 테이블에 레코드 없음

---

## 🔍 원인 분석

### 근본 원인
**등록된 결제 수단 기능이 미완성 상태**

#### 코드 분석 결과

**1. cart.html (669-730행)**
```javascript
async function processSavedPaymentMethod(methodId) {
    // TODO: 빌링키를 사용한 자동 결제 API 호출
    // 현재는 주문만 생성하고, 실제 빌링키 결제는 추후 구현

    const response = await fetch('/api/mobile/orders/create/', {
        // ❌ 결제 API 호출 없음
    });
}
```

**2. api_views.py (709-811행)**
```python
def api_order_create(request):
    # 주문만 생성
    order = Order.objects.create(
        payment_status='unpaid'  # ❌ 미결제 상태
    )
    # ❌ Payment 레코드 생성 안함
    # ❌ 토스페이먼츠 API 호출 안함
    # ✓ 재고는 차감함 (문제!)
```

**3. api_views_mobile.py (529행)**
```python
# 가짜 빌링키 생성
billing_key_temp = f"TEMP_{customer_code}_{card_number[-4:]}"
# ❌ 실제 토스페이먼츠 빌링키 API 호출 안함
```

### 정상 결제 vs 문제 결제 비교

| 구분 | 10월 31일 (성공) | 11월 11일 (실패) |
|------|------------------|------------------|
| 결제 방법 | "새 카드로 결제" | "등록된 카드" (MasterCard 4958) |
| API | `/api/mobile/payment/prepare/` → 토스페이먼츠 → `/api/mobile/payment/confirm/` | `/api/mobile/orders/create/` |
| Payment 레코드 | ✅ 생성됨 | ❌ 없음 |
| payment_key | ✅ tirep20251031... | ❌ NULL |
| 실제 결제 | ✅ 완료 | ❌ 진행 안됨 |
| 결과 | 6건 모두 성공 | 2건 모두 실패 |

---

## ✅ 조치 내용

### 1. cart.html 수정 (등록된 카드 기능 비활성화)

**파일**: `tire_data/templates/mobile/cart.html`

**수정 내용**:
```javascript
// 535행: 등록된 결제 수단 옵션 숨김
${false && paymentMethods.length > 0 ? paymentMethods.map(...

// 562-573행: 경고 메시지 추가
⚠️ 등록된 결제 수단은 현재 사용할 수 없습니다.
안전한 결제를 위해 "새 카드로 결제" 옵션을 이용해주세요.
(시스템 개선 작업 중입니다)
```

**결과**: 사용자는 더 이상 등록된 카드를 선택할 수 없음

---

### 2. 미결제 주문 취소

**실행 위치**: PythonAnywhere MySQL 콘솔

**SQL**:
```sql
UPDATE orders
SET order_status = 'cancelled',
    payment_status = 'cancelled'
WHERE order_number IN ('ORD20251111008', 'ORD20251111009');
```

**결과**: 2건 모두 취소 처리 완료

---

### 3. 재고 복구

**상품 정보**:
- 상품코드: K-LT-S036
- 상품명: 145R13 8L POWER GRIP KC12
- 제조년도: 2025년
- 복구 수량: 2개

**SQL**:
```sql
UPDATE year_allocations
SET year_2025 = year_2025 + 2
WHERE goods_code = 'K-LT-S036';
```

**결과**:
- Before: 179개
- After: 181개 (+2)
- ✅ 재고 복구 완료

---

### 4. 고객 안내문 작성

**파일**: `광주_업체_최종_안내문.txt`

**주요 내용**:
- 문제 상황 설명
- 조치 완료 사항 안내
- 재주문 방법 상세 안내
- "새 카드로 결제"만 사용 요청

---

## 📈 Database 상태

### Orders 테이블
```
ORD20251111008: order_status=cancelled, payment_status=cancelled
ORD20251111009: order_status=cancelled, payment_status=cancelled
```

### Payments 테이블
```
두 주문 모두 Payment 레코드 없음 (정상)
```

### year_allocations 테이블
```
K-LT-S036: year_2025 = 181 (복구 완료)
```

### payment_methods 테이블
```
id=3: customer_code=0-0-0002, billing_key=TEMP_0-0-0002_4958
(가짜 빌링키, 사용 불가능)
```

---

## 🎯 향후 계획

### 단기 (현재 상태 유지)
- ✅ 등록된 결제 수단 기능 비활성화 상태 유지
- ✅ "새 카드로 결제"만 사용 가능
- ✅ 추가 피해 방지

### 장기 (토스페이먼츠 빌링키 구현)

**필요한 작업**:

1. **카드 등록 시 실제 빌링키 발급**
   ```python
   # api_views_mobile.py 수정
   # 토스페이먼츠 빌링키 발급 API 호출
   response = requests.post(
       'https://api.tosspayments.com/v1/billing/authorizations/issue',
       ...
   )
   billing_key = response.json()['billingKey']  # 실제 빌링키
   ```

2. **등록된 카드로 결제 시 빌링키 자동결제**
   ```javascript
   // cart.html 수정
   async function processSavedPaymentMethod(methodId) {
       // TODO 제거하고 실제 구현
       const response = await fetch('/api/mobile/payment/billing/', {
           // 빌링키 자동결제 API
       });
   }
   ```

3. **Payment 레코드 정상 생성**
   ```python
   # 빌링키 결제 API
   def api_payment_billing(request):
       # 토스페이먼츠 빌링키 결제 API 호출
       # Payment 레코드 생성
       # 주문 상태 업데이트
   ```

**참고 문서**:
- https://docs.tosspayments.com/reference/widget-sdk#빌링키-발급
- https://docs.tosspayments.com/guides/billing

---

## 📝 교훈

### 발견된 문제점
1. **TODO 주석이 실제 코드로 배포됨**
   - 개발 미완성 기능이 프로덕션에 노출

2. **결제 없이 주문 생성 + 재고 차감**
   - 심각한 재고 관리 오류 발생 가능

3. **가짜 빌링키 저장**
   - `TEMP_` 접두사로 실제 사용 불가능한 빌링키

### 개선 방향
1. **기능 플래그 (Feature Flag) 사용**
   - 미완성 기능은 코드 레벨에서 비활성화

2. **결제 트랜잭션 원자성 보장**
   - 결제 성공 시에만 재고 차감

3. **빌링키 유효성 검증**
   - 실제 토스페이먼츠 빌링키인지 확인

---

## 🔗 관련 파일

### 수정된 파일
- `tire_data/templates/mobile/cart.html` (등록된 카드 비활성화)

### 참고 파일
- `tire_data/api_views.py` (결제 API)
- `tire_data/api_views_mobile.py` (결제 수단 등록)
- `toss_payment_apis.py` (토스페이먼츠 API - 사용 안됨)

### 생성된 문서
- `작업완료_보고서_20251111.md` (본 문서)
- `광주_업체_최종_안내문.txt` (고객 안내)
- `pythonanywhere_correct_queries.sql` (DB 조회 쿼리)
- `restore_inventory.sql` (재고 복구 쿼리)
- `cancel_unpaid_orders.py` (주문 취소 스크립트)
- `cancel_unpaid_orders.sql` (주문 취소 쿼리)

---

## ✅ 체크리스트

- [x] 문제 원인 파악
- [x] cart.html 수정 (등록된 카드 비활성화)
- [x] 미결제 주문 취소
- [x] 재고 복구
- [x] 고객 안내문 작성
- [ ] Git 커밋 & 푸시
- [ ] PythonAnywhere 배포
- [ ] 고객 안내 발송

---

**작성일**: 2025년 11월 11일
**최종 업데이트**: 재고 복구 완료
