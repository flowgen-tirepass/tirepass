# TirePASS 쇼핑 플로우 가이드

TirePASS 모바일 앱의 전체 쇼핑 프로세스 가이드입니다.

## 📋 목차
1. [전체 플로우 개요](#전체-플로우-개요)
2. [단계별 상세 가이드](#단계별-상세-가이드)
3. [테스트 시나리오](#테스트-시나리오)
4. [API 엔드포인트](#api-엔드포인트)
5. [문제 해결](#문제-해결)

---

## 전체 플로우 개요

```
1. 회원가입/로그인
   ↓
2. 상품 검색/브라우징
   ↓
3. 상품 상세 보기
   ↓
4. 장바구니 담기 (DOT 년도 선택)
   ↓
5. 장바구니 확인/수정
   ↓
6. 주문하기 클릭
   ↓
7. 배송지 선택/추가 + 배송 메모 입력
   ↓
8. 결제 수단 선택 (카드/계좌이체)
   ↓
9. 결제 진행
   ↓
10. 주문 완료
   ↓
11. 주문 내역 확인
   ↓
12. 주문 취소 (선택)
```

---

## 단계별 상세 가이드

### 1️⃣ 회원가입 및 로그인

#### 회원가입 (`/mobile/register/`)
- **필수 정보**:
  - 사업자등록번호 (10자리)
  - 상호
  - 비밀번호 (최소 4자)
- **선택 정보**:
  - 대표자
  - 전화번호
  - 휴대전화

#### 로그인 (`/mobile/login/`)
- 사업자등록번호 + 비밀번호
- 로그인 성공 시 `customer_code`를 localStorage에 저장

---

### 2️⃣ 상품 검색 및 브라우징 (`/mobile/products/`)

#### 검색 기능
- **타이어 사이즈 검색**:
  - 숫자만 입력 시 자동 변환 (예: `2355519` → `235/55R19`)
  - 슬래시 포함 검색 (예: `235/55R19`)

#### 필터링
- 브랜드 선택 (미쉐린, 금호, 넥센 등)
- 여러 브랜드 동시 선택 가능

#### 정렬
- 가격 낮은 순
- 가격 높은 순
- 재고 많은 순
- 최신 순

---

### 3️⃣ 상품 상세 보기 (`/mobile/products/<code>/`)

#### 표시 정보
- 상품명, 브랜드, 코드
- 기본가격 및 할인가
- 재고 수량
- DOT 년도별 재고 및 할인율
- 성능 태그 (조용함, 연비, 눈길/빗길 등)

#### 액션
- 장바구니 담기 (년도 선택 가능)
- 바로 구매

---

### 4️⃣ 장바구니 담기

#### DOT 년도 선택
```javascript
// 년도별 재고 및 할인율
2025: 재고 100개 (할인 0%)
2024: 재고 50개 (할인 3%)
2023: 재고 20개 (할인 5%)
2022: 재고 10개 (할인 8%)
2021 이전: 재고 5개 (할인 10%)
```

#### API 호출
```
POST /api/mobile/cart/add/
{
  "customer_code": "C001",
  "product_code": "M-CC2SUV-07",
  "quantity": 4,
  "selected_year": "2024"
}
```

---

### 5️⃣ 장바구니 확인 (`/mobile/cart/`)

#### 기능
- **수량 조정**: +/- 버튼으로 수량 변경
- **삭제**: ❌ 버튼으로 개별 상품 삭제
- **가격 확인**:
  - 단가 표시
  - 할인율 표시
  - 최종 가격 계산

#### 총 결제 금액
- 모든 상품의 할인 적용 가격 합계

---

### 6️⃣ 주문 프로세스

#### Step 1: 배송지 선택 (`주문하기` 클릭)
```
┌─────────────────────────┐
│  배송지 선택             │
├─────────────────────────┤
│ ○ 홍길동 (기본)         │
│   010-1234-5678         │
│   서울시 강남구...      │
│                         │
│ ○ 김철수               │
│   010-9876-5432         │
│   경기도 성남시...      │
├─────────────────────────┤
│ 배송 메모 (선택)        │
│ ┌─────────────────────┐ │
│ │ 문 앞에 놔주세요    │ │
│ └─────────────────────┘ │
├─────────────────────────┤
│ [취소]  [다음]          │
└─────────────────────────┘
```

- **배송지 관리**: `/mobile/addresses/`에서 추가/수정/삭제
- **기본 배송지**: 자동 선택
- **배송 메모**: 선택 입력 (예: "문 앞에 놔주세요", "배송 전 연락주세요")

#### Step 2: 결제 수단 선택
```
┌─────────────────────────┐
│  결제 수단 선택          │
├─────────────────────────┤
│ ● 💳 카드결제           │
│   토스페이먼츠로 안전하게│
│                         │
│ ○ 🏦 계좌이체           │
│   주문 후 계좌번호 안내  │
├─────────────────────────┤
│ [이전]  [결제하기]      │
└─────────────────────────┘
```

---

### 7️⃣ 결제 진행

#### 카드 결제 (토스페이먼츠)
1. 결제창 팝업
2. 카드 정보 입력 (테스트 카드 사용)
3. 본인 인증
4. 결제 승인
5. 결제 완료 페이지로 리다이렉트

#### 계좌이체
1. 주문 즉시 생성
2. 주문 완료 페이지 표시
3. 관리자가 입금 확인 후 주문 확인

---

### 8️⃣ 주문 내역 (`/mobile/orders/`)

#### 주문 목록
- 주문번호
- 주문일시
- 총 금액
- 주문 상태 (주문대기, 주문확인, 출고준비, 배송중, 배송완료, 주문취소)
- 결제 상태 (미결제, 결제완료, 환불완료)

#### 주문 상세 (`/mobile/orders/<id>/`)
- 주문 정보 (주문번호, 일시, 상태)
- 주문 상품 목록 (상품명, 수량, 가격, 할인)
- 결제 정보 (결제 수단, 금액)
- 배송 정보 (배송지, 메모)
- **주문 취소 버튼** (배송 완료 전까지 가능)

---

### 9️⃣ 주문 취소

#### 취소 가능 조건
- ✅ 주문대기, 주문확인, 출고준비 상태
- ❌ 배송중, 배송완료 상태

#### 취소 프로세스
1. 주문 상세에서 "주문 취소" 버튼 클릭
2. 확인 팝업
3. 서버에서 주문 상태 변경 (`cancelled`)
4. 카드 결제인 경우: 토스페이먼츠 취소 API 호출
5. 재고 복구
6. 완료 메시지

---

## 테스트 시나리오

### 시나리오 1: 정상 주문 (카드 결제)
```
1. 로그인 (테스트 계정)
2. "미쉐린" 브랜드 검색
3. "235/55R19" 타이어 선택
4. 2024년 DOT 선택 (할인 3%)
5. 장바구니 담기 (4개)
6. 장바구니 확인
7. 주문하기 클릭
8. 배송지 선택 (홍길동)
9. 배송 메모 입력: "문 앞에 놔주세요"
10. 카드결제 선택
11. 테스트 카드 입력: 5514-1234-5678-9012
12. 결제 승인
13. 주문 완료 확인
```

### 시나리오 2: 승인 거부 테스트
```
1-10. 시나리오 1과 동일
11. 승인 거부 카드 입력: 5514-0000-0000-0001
12. 결제 실패 메시지 확인
13. 주문 상태: cancelled
```

### 시나리오 3: 주문 취소
```
1-13. 시나리오 1 완료
14. 주문 내역에서 주문 상세 보기
15. "주문 취소" 버튼 클릭
16. 확인 팝업에서 "확인"
17. 취소 완료 메시지 확인
18. 주문 상태: cancelled
19. 결제 상태: refunded (카드 결제인 경우)
```

### 시나리오 4: 배송지 관리
```
1. 프로필 페이지 접속
2. "배송지 관리" 클릭
3. "새 배송지 추가" 클릭
4. 배송지 정보 입력
   - 수령인: 김철수
   - 전화번호: 010-9876-5432
   - 주소: 경기도 성남시 분당구
   - 상세주소: 101동 1001호
5. "기본 배송지로 설정" 체크
6. 저장
7. 기존 기본 배송지는 자동으로 해제됨
```

---

## API 엔드포인트

### 인증
- `POST /api/mobile/auth/register/` - 회원가입
- `POST /api/mobile/auth/login/` - 로그인
- `POST /api/mobile/auth/logout/` - 로그아웃
- `GET /api/mobile/auth/profile/` - 프로필 조회

### 상품
- `GET /api/mobile/products/` - 상품 목록
- `GET /api/mobile/products/<code>/` - 상품 상세

### 장바구니
- `GET /api/mobile/cart/` - 장바구니 조회
- `POST /api/mobile/cart/add/` - 장바구니 담기
- `POST /api/mobile/cart/<id>/update/` - 수량 수정
- `DELETE /api/mobile/cart/<id>/remove/` - 삭제

### 주문
- `GET /api/mobile/orders/` - 주문 목록
- `POST /api/mobile/orders/create/` - 주문 생성
- `GET /api/mobile/orders/<id>/` - 주문 상세
- `POST /api/mobile/orders/<id>/cancel/` - 주문 취소

### 배송지
- `GET /api/mobile/shipping-addresses/` - 배송지 목록
- `POST /api/mobile/shipping-addresses/add/` - 배송지 추가
- `POST /api/mobile/shipping-addresses/<id>/update/` - 배송지 수정
- `DELETE /api/mobile/shipping-addresses/<id>/delete/` - 배송지 삭제
- `POST /api/mobile/shipping-addresses/<id>/set-default/` - 기본 배송지 설정

### 결제 (토스페이먼츠)
- `POST /api/mobile/payment/prepare/` - 결제 준비
- `POST /api/mobile/payment/confirm/` - 결제 승인
- `POST /api/mobile/payment/cancel/` - 결제 취소
- `GET /api/mobile/payment/status/<payment_key>/` - 결제 상태 조회

---

## 문제 해결

### 1. 장바구니가 비어있다고 표시됨
```
원인: customer_code가 localStorage에 없음
해결: 로그아웃 후 다시 로그인
```

### 2. 상품 검색이 안됨
```
원인: ERP API 연결 실패
해결:
1. ERP 서버 상태 확인 (http://itire2.iptime.org:8002/goods)
2. 로그 확인 (debug.log)
```

### 3. 결제창이 뜨지 않음
```
원인: 토스페이먼츠 SDK 로드 실패
해결:
1. 네트워크 확인
2. 브라우저 콘솔에서 에러 확인
3. CLIENT_KEY 확인
```

### 4. 주문 취소가 안됨
```
원인: 이미 배송 완료된 주문
해결: 배송 완료 전까지만 취소 가능
```

### 5. 배송지가 저장되지 않음
```
원인: 필수 항목 미입력
해결: 수령인, 전화번호, 주소는 필수 입력
```

---

## 테스트 계정 정보

### 테스트 계정
```
사업자등록번호: 1234567890
비밀번호: test1234
```

### 테스트 카드 (프로필 페이지에서 확인 가능)
```
✅ 정상 승인: 5514-1234-5678-9012 (유효기간: 12/25, CVC: 123)
❌ 승인 거부: 5514-0000-0000-0001 (유효기간: 12/30, CVC: 123)
```

---

## 추가 정보

### 할인 계산 로직
```
최종가격 = 단가
         × (1 - 기본할인/100)
         × (1 - 고객브랜드할인/100)
         × (1 - 고객추가할인/100)
         × (1 - DOT할인/100)
```

### 재고 관리
- 주문 생성 시: 재고 차감
- 주문 취소 시: 재고 복구
- 년도별 재고 별도 관리 (year_allocations 테이블)

### 주문 상태 변화
```
pending (주문대기)
  → confirmed (주문확인)
  → preparing (출고준비)
  → shipped (배송중)
  → delivered (배송완료)

취소 시: cancelled
```

---

**마지막 업데이트:** 2025-01-13
**작성자:** TirePASS 개발팀
**문의:** support@tirepass.com
